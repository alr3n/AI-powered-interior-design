// PLACEHOLDER — replace by running:  flutterfire configure
// (docs/05-IMPLEMENTATION_GUIDE.md §3). The FlutterFire CLI generates this
// file with your project's real keys; Firebase client config values are not
// secrets, but the Gemini key must NEVER appear here or anywhere in the app.
import 'package:firebase_core/firebase_core.dart' show FirebaseOptions;

class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    throw UnsupportedError(
      'firebase_options.dart is a placeholder. '
      'Run `flutterfire configure` to generate the real file.',
    );
  }
}
